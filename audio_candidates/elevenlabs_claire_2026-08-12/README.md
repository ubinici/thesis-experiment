# ElevenLabs Claire cue candidates

Source recording:

`ElevenLabs_2026-08-12T13_49_03_Claire - Ultra Real & Natural_pvc_sp100_s50_sb75_se0_b_m2.mp3`

Generated on 2026-08-12 with ElevenLabs and cropped on 2026-08-12.

Each WAV contains only `Click on the [colour]`. The source contained the full
continuation `circle beside the square ...`; the files end at the acoustically
inspected adjective offset, before the initial `/s/` of `circle`. Cuts were moved
to the immediately preceding zero crossing to avoid a digital click. No trailing
silence was added.

Format: mono, 44.1 kHz, 16-bit PCM WAV. The original project cues in
`chunk_includes` were not overwritten.

`crop_manifest.csv` records source positions, actual output durations, levels,
and boundary notes. The `orange` boundary is less certain than the others because
the final `/dʒ/` of *orange* and initial `/s/` of *circle* form a continuous
fricative sequence. Review that cue especially carefully before deployment.

Automated verification transcribed all seven outputs as `Click on the [colour]`
without detecting the following noun. Final acceptance still requires listening
and a small blinded naturalness/continuation pretest.
